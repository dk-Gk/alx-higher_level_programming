#!/usr/bin/python3
def best_score(a_dictionary):
    for i in a_dictionary:
        if not a_dictionary[i]:
            return None
        big = a_dictionary[i]
        a = i
    for i in a_dictionary:
        if a_dictionary[i] > big:
            big = a_dictionary[i]
            a = i
    return a
